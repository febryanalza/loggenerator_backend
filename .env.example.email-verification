# ========================================
# CHECKLIST: Required Environment Variables for Email Verification
# ========================================
# 
# ✅ Variables yang HARUS ada untuk Email Verification bekerja:
# 
# 1. APP_URL - URL aplikasi untuk generate verification link
# 2. MAIL_MAILER - Harus 'smtp' (bukan 'log')
# 3. MAIL_HOST - SMTP server Brevo
# 4. MAIL_PORT - Port SMTP (587 untuk TLS)
# 5. MAIL_USERNAME - Username SMTP Brevo
# 6. MAIL_PASSWORD - Password SMTP Brevo
# 7. MAIL_ENCRYPTION - TLS atau SSL
# 8. MAIL_FROM_ADDRESS - Email pengirim (harus verified di Brevo)
# 9. MAIL_FROM_NAME - Nama pengirim
# 
# ========================================

# ========================================
# APPLICATION CONFIGURATION
# ========================================
APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_TIMEZONE=UTC

# ⚠️ IMPORTANT: Set this to your actual domain in production
# This is used to generate email verification links
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

# ========================================
# LOGGING CONFIGURATION
# ========================================
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# ========================================
# DATABASE CONFIGURATION
# ========================================
DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=loggenerator
DB_USERNAME=postgres
DB_PASSWORD=

# ========================================
# SESSION CONFIGURATION
# ========================================
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# ========================================
# BROADCASTING CONFIGURATION
# ========================================
BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

# Broadcasting Configuration (for realtime notifications)
BROADCAST_DRIVER=pusher
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

# ========================================
# CACHE CONFIGURATION
# ========================================
CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

# ========================================
# REDIS CONFIGURATION
# ========================================
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# ========================================
# MAIL CONFIGURATION (BREVO/SENDINBLUE)
# ========================================
# ⚠️ CRITICAL: For email verification to work:
# 1. MAIL_MAILER must be 'smtp' (NOT 'log')
# 2. Fill in all Brevo SMTP credentials
# 3. MAIL_FROM_ADDRESS must be verified in Brevo dashboard

# Mail Driver - MUST be 'smtp' for email sending to work
# Options: smtp, sendmail, mailgun, ses, postmark, log, array, failover, roundrobin
# For Brevo (Sendinblue), use 'smtp'
MAIL_MAILER=smtp

# Brevo SMTP Server Configuration
MAIL_HOST=smtp-relay.brevo.com
MAIL_PORT=587

# Your Brevo SMTP Username (usually in format: xxxxxxxx@smtp-brevo.com)
# Get this from: Brevo Dashboard → SMTP & API → SMTP
MAIL_USERNAME=your_brevo_smtp_username@smtp-brevo.com

# Your Brevo SMTP Password (API Key)
# Get this from: Brevo Dashboard → SMTP & API → SMTP
MAIL_PASSWORD=your_brevo_smtp_password

# Encryption Method - Use 'tls' for port 587 or 'ssl' for port 465
# Recommended: tls
MAIL_ENCRYPTION=tls

# Optional: Specific mail scheme (usually not needed)
MAIL_SCHEME=null

# Sender Email Address (MUST be verified in Brevo)
# Go to: Brevo Dashboard → Senders → Add Sender
# Note: Fix typo 'noreplay' → 'noreply'
MAIL_FROM_ADDRESS="noreply@yourdomain.com"

# Sender Name (will appear as "from" in emails)
MAIL_FROM_NAME="${APP_NAME}"

# ========================================
# AWS CONFIGURATION (if using AWS SES instead of Brevo)
# ========================================
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# ========================================
# GOOGLE AUTHENTICATION CONFIGURATION
# ========================================
# Google OAuth Client IDs for multi-platform support
# Get these from: Google Cloud Console → APIs & Services → Credentials

# Web Client ID (for web applications)
GOOGLE_CLIENT_ID=your_web_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_web_client_secret

# Android Client ID (for Android app)
GOOGLE_ANDROID_CLIENT_ID=your_android_client_id.apps.googleusercontent.com

# iOS Client ID (for iOS app) 
GOOGLE_IOS_CLIENT_ID=your_ios_client_id.apps.googleusercontent.com

# ========================================
# ADMIN AUTHENTICATION CONFIGURATION
# ========================================
# Token expiration time in hours for admin users
ADMIN_TOKEN_EXPIRATION_HOURS=4

# ========================================
# FRONTEND APPLICATION
# ========================================
VITE_APP_NAME="${APP_NAME}"

# ========================================
# ADDITIONAL NOTES FOR EMAIL VERIFICATION
# ========================================
#
# SETUP STEPS:
# ------------
# 1. Create Brevo Account (https://www.brevo.com)
# 2. Verify your sender email address in Brevo Dashboard
# 3. Get SMTP credentials from Brevo Dashboard → SMTP & API
# 4. Update these .env variables with your actual credentials
# 5. Test email sending: php artisan tinker
#    >>> Mail::raw('Test', function($m) { $m->to('test@example.com')->subject('Test'); });
#
# TROUBLESHOOTING:
# ----------------
# - If emails not sending: Check MAIL_MAILER is 'smtp' not 'log'
# - If authentication fails: Verify MAIL_USERNAME and MAIL_PASSWORD
# - If sender rejected: Make sure MAIL_FROM_ADDRESS is verified in Brevo
# - Check logs: storage/logs/laravel.log
#
# PRODUCTION CHECKLIST:
# ---------------------
# ✅ Set APP_ENV=production
# ✅ Set APP_DEBUG=false
# ✅ Set APP_URL to your actual domain (https://yourdomain.com)
# ✅ Use production MAIL credentials
# ✅ Test email verification flow end-to-end
# ✅ Enable HTTPS for secure email links
# ✅ Set proper MAIL_FROM_ADDRESS (your verified domain email)
#
